#include <stdio.h>
int main (){
    int x = 50;
    if (x < 50){
    printf("x is smaller than 50");
    } else if (x > 50) {
    printf("x is larger than 50");
    } else {
    printf("Doesn't matter :3");
    }
    return 0;
}
